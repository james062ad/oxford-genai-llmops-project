[pytest]
# Only look for tests in the Tests/ directory
testpaths = Tests

# Only consider files named test_*.py
python_files = test_*.py

# Add project root (.) to sys.path so imports like `import basic_functions` work
pythonpath = src
